services:
  apache:
    build: .
    restart: always
    ports:
      - "8080:80"
    volumes:
      - ../dist:/var/www/html
    healthcheck:
      test: ["CMD", "wget", "--spider", "--quiet", "http://apache/api/health"]
      start_period: 10s
